from django.urls import path
from . import views

urlpatterns = [
    # path('register', views.createUser, name='create_users'),
    # path('login', views.loginUser, name='login')
    # path('admin/', admin.site.urls),
]
